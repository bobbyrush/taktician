/**
 * Centralized port configuration for Taktician
 *
 * Re-exports from @taktician/types for backward compatibility.
 * The canonical definition is in @taktician/types to allow browser-safe imports.
 */

export { STATIC_PORT, SERVER_PORT, RESERVED_PORTS } from '@taktician/types';
